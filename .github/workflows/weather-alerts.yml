      - name: Проверяем опасную погоду
        id: check_danger
        env:
          # Важно: Используйте то же имя секрета, что и в daily-weather.yml
          WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}
        run: |
          # Получаем текущую погоду от Visual Crossing API
          RESPONSE=$(curl -s "https://weather.visualcrossing.com/VisualCrossingWebServices/rest/services/timeline/kazan?unitGroup=metric&include=current&key=$WEATHER_API_KEY&contentType=json")

          # Извлекаем данные из ОТВЕТА Visual Crossing
          TEMP=$(echo $RESPONSE | jq -r '.currentConditions.temp')
          WIND_SPEED=$(echo $RESPONSE | jq -r '.currentConditions.windspeed')
          CONDITIONS=$(echo $RESPONSE | jq -r '.currentConditions.conditions')
          DESCRIPTION=$(echo $RESPONSE | jq -r '.currentConditions.conditions') # У Visual Crossing часто conditions == description

          # Проверяем опасные условия
          ALERT=""

          if (( $(echo "$TEMP >= 35" | bc -l) )); then
            ALERT+="*Аномальная жара!* 🔥\nТемпература: ${TEMP}°C\n\n"
            echo "has_alert=true" >> $GITHUB_OUTPUT
          fi

          if (( $(echo "$TEMP <= -20" | bc -l) )); then
            ALERT+="*Сильный мороз!* ❄️\nТемпература: ${TEMP}°C\n\n"
            echo "has_alert=true" >> $GITHUB_OUTPUT
          fi

          if (( $(echo "$WIND_SPEED >= 15" | bc -l) )); then
            ALERT+="*Сильный ветер!* 💨\nСкорость: ${WIND_SPEED} м/с\n\n"
            echo "has_alert=true" >> $GITHUB_OUTPUT
          fi

          # Проверяем условия по ключевым словам Visual Crossing
          if [[ "$CONDITIONS" == *"Thunderstorm"* ]] || [[ "$CONDITIONS" == *"Storm"* ]]; then
            ALERT+="*Гроза!* ⛈️\n$DESCRIPTION\n\n"
            echo "has_alert=true" >> $GITHUB_OUTPUT
          fi

          if [[ "$CONDITIONS" == *"Snow"* ]] && (( $(echo "$TEMP <= -10" | bc -l) )); then
            ALERT+="*Сильный снегопад!* 🚨\n$DESCRIPTION\n\n"
            echo "has_alert=true" >> $GITHUB_OUTPUT
          fi

          echo "alert_message<<EOF" >> $GITHUB_OUTPUT
          echo "$ALERT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          # Если нет предупреждений
          if [ -z "$ALERT" ]; then
            echo "has_alert=false" >> $GITHUB_OUTPUT
          fi
